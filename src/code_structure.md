

Managers are global objects that interact with eachother using rumors, systemManager is the main manager
Managers run in a thread and have a rumor queue, handlers only activate when invoked (by a manager)

